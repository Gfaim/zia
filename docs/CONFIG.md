# Required:

## Sources

In order to load modules, you will have to have the `sources` variable set at the root of the YAML config file, containing an array of folders that will be used to search dynamic libraries that has modules within.

Here is a little exemple:

```yaml
...
sources:
    - modules
    - data/external/modules
...
```

## Module variables

# Optional